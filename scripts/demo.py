#!/usr/bin/env python3
"""`make demo` -- headless interview: seed -> session -> audio turn -> report.

Placeholder until the API (T09) and the durable workflow (T07) exist.
"""

from __future__ import annotations

import sys


def main() -> int:
    print("demo: not wired yet -- lands once the API (T09) is on master", file=sys.stderr)
    return 1


if __name__ == "__main__":
    sys.exit(main())
